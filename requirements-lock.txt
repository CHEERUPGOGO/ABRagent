# requirements-lock.txt — 已验证可用的精确版本锁定 (constraints 语义)
#
# 用法 (作为约束安装，不引入额外包，只锁定版本):
#   pip install -e ".[all]" -c requirements-lock.txt
#
# 锁定环境: Python 3.13.5 / Windows · 基线: 41 passed, 0 warnings
# 升级依赖后请重新生成并跑: pytest -m "unit or not external"
# 注意: pybamm 物理仿真需 Python < 3.13，未包含在本锁定环境内

# ---- 核心运行时 (base) ----
requests==2.34.2
pyyaml==6.0.3
beautifulsoup4==4.14.3
pydantic==2.13.4
jinja2==3.1.6
tqdm==4.67.1
rich==15.0.0
numpy==2.4.3
pandas==3.0.1
matplotlib==3.10.8
openai==2.46.0
langchain-core==1.5.4
langchain-openai==1.3.5
langchain==1.3.15
langgraph==1.2.11
tiktoken==0.13.0
fastapi==0.141.1
uvicorn==0.52.1
# rank-bm25: 锁定环境未安装 (BM25 检索为可选路径，缺失时走 TF-IDF)

# ---- RAG 扩展 [rag] ----
langchain-chroma==1.1.0
langchain-ollama==1.1.0
chromadb==1.5.9
ollama==0.6.2

# ---- UI 扩展 [ui] ----
textual==8.2.8
gradio==6.26.0

# ---- 开发扩展 [dev] ----
pytest==9.1.1
pytest-asyncio==1.4.0
